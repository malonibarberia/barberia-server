name: Ping Render App Every 5 Minutes

on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping backend on Render
        run: curl --fail --silent --show-error --max-time 15 https://barberia-server.onrender.com/ping
